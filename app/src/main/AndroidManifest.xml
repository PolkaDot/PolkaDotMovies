<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.polka.pdm">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>
        <!-- <activity android:name=".StartApplication" /> -->
        <activity
            android:name=".LoginApplication"
            android:label="@string/login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegistrationPage"
            android:label="@string/title_activity_registration_page"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".ViewProfile"
            android:label="@string/viewProfile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".EditProfile"
            android:label="@string/editProfile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".GooglePlacesActivity"
            android:label="@string/title_activity_Google_Places"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HomeApp"
            android:label="@string/title_activity_home_app"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RecentDvds"
            android:label="@string/title_activity_recent_dvds"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".SearchMovies"
            android:label="@string/searchMovies"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ReviewPage"
            android:label="@string/title_activity_review_page"
            android:theme="@style/AppTheme.NoActionBar" />
        android:theme="@style/AppTheme.NoActionBar"/>
        <activity
            android:name=".RecentMovies"
            android:label="@string/title_activity_recent_movies"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ViewMovie"
            android:label="@string/title_activity_view_movie"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BlockUser"
            android:label="@string/title_activity_block_user"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".RecommendMovie"
            android:label="@string/title_activity_recommend_movie"></activity>
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDLLytOp_KeO5gpHKaZI5dT1P6rWnGIp0I" />

    </application>

</manifest>